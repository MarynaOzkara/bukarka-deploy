import { SortButton } from "./SortFilter.styled";

const Sort = () => {
  return (
    <SortButton>
      <span className="material-symbols-outlined">sort</span>
    </SortButton>
  );
};

export default Sort;
