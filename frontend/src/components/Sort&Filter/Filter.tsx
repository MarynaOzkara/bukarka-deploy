import { SortButton } from "./SortFilter.styled";

const Filter = () => {
  return (
    <SortButton>
      <span className="material-symbols-outlined">filter_list</span>
    </SortButton>
  );
};

export default Filter;
